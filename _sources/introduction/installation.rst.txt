Installation
============

In the future we plan to distribute our packages via PyPi, but for now you will have to install the package from our source code.

Instruction:

#. clone our repository
#. go into the local directory containing the repository content and type ``pip install .``
